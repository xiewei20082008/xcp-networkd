(jbuild_version 1)

(library
 ((name networklibs)
  (flags (:standard :standard -bin-annot -safe-string))
  (libraries (astring
              forkexec
              rpclib
              re.perl
              systemd
              threads
              xapi-stdext-unix
              xcp-inventory
              xcp.network))
  (wrapped false)
 )
)